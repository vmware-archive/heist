=================================
Heis SSH Management and Discovery
=================================

Heis is a migration of the concepts around salt-ssh into a stanalone project. This
is not about just puling salt-ssh out of Salt, but instead about modernizing 
salt-ssh to work with Python 3.6, `pop` and `asyncio`
